// config.js - stores API endpoints and global settings
export const CMS_CONFIG = {
    // When ENABLED is true, the site fetches content from the Strapi API.
    // When false, it uses the static HTML content (fallback).
    ENABLED: true,

    // Strapi API URL (production)
    API_URL: 'https://yoani-springs-cms.onrender.com/api',

    // If you deploy Strapi to a server, change this to the live URL.
    // Example: 'https://api.yoanisprings.ac.ke/api'
};